# AGENTS.md
Codex 的仓库级说明。
## 文档编码
- 本项目文档统一使用中文；读取文档时请使用 UTF-8 编码。
## 防御性编码限制
- 不要为**不可能发生的情况**添加错误处理、回退或验证。信任内部代码和框架的保证。**只在系统边界进行验证**（用户输入、外部 API）。
## 上下文限制
- 如果上下文限制或对话压缩导致关键信息缺失，并可能引发误判或幻觉时，请主动提醒用户开新会话后再继续。
## 回复质量
- 对于用户提出的问题，不必顺着用户给出的思路；应结合你的知识和判断，给出更合适、更高质量的答案。
- 编写代码时，代码注释部分除了必要的英文单词和句子之外，必须使用中文。
## LangGraph 问题
- 涉及 LangGraph 时，先查阅官方文档：https://docs.langchain.com/oss/python/langgraph
- 回答应优先依据官方文档，再补充解释或建议。
## 新技术与工具
- 涉及新技术、新工具，或可能已变化的信息时，先进行网页搜索再回答。
- 优先使用最新且权威的来源，确保答案准确、及时。
## Windows 命令行
- 用户电脑运行的是 Windows。
- 提供命令行说明时，请使用适合 Windows 的命令。
- 在新的终端会话中，执行第一个项目命令前先激活 Conda 环境：

```powershell
conda activate midea
```
- 如需执行多个命令，默认先完成环境激活步骤。

## 测试超时
- 本仓库全量后端回归 `pytest -q` 当前可能运行 12 分钟以上；执行完整测试时，工具超时请设置至少 900000ms（15 分钟），不要使用默认 120s 或 600s。
- 定向测试仍可使用 120000ms；只有全量 `pytest -q` 或接近全量的后端回归需要长超时。
- 如果首次全量测试因工具 timeout 被终止且没有失败输出，先用更长 timeout 重跑，不要把它当成测试失败或据此修改代码。
