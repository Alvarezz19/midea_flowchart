# 项目结构

本文档用于新对话或新协作者快速熟悉 `midea_flowchart` 项目。优先阅读本文，再按需要查看 README、代码和生成报告。

## 项目目标

本项目用于读取美的暖通/楼宇控制领域的图形化控制程序 JSON，并生成用户友好的模块级流程图。

核心目标不是把所有原始节点完整摊开，而是：

- 从 `programs` 中解析原始 JSON 项目代码。
- 根据命名规则表校验点位命名。
- 将节点级控制程序抽象为模块级流程图。
- 保留原始节点追溯能力，工程人员能回查到 JSON 节点。
- 用 React + React Flow 展示总览图和页面图。

## 当前处理流程

```text
programs/*.json
  -> Python 解析器
  -> build/intermediate/*.json
  -> 抽象图生成器
  -> build/diagrams/*.json
  -> web/public/data/*
  -> React Flow 前端展示
```

命名规则处理流程：

```text
文档/1.命名规则v20191011B.XLSX
  -> data/naming_rules.sqlite
  -> data/naming_rules.json
  -> 点位命名匹配、未匹配点位统计、模块风险提示
```

## 根目录结构

```text
.
├─ AGENTS.md
├─ README.md
├─ 项目结构.md
├─ 示例.png
├─ programs/
├─ 文档/
├─ configs/
├─ midea_flowchart/
├─ tests/
├─ data/
├─ build/
└─ web/
```

## 关键目录说明

### `programs/`

存放原始项目 JSON，是本项目的输入数据。

当前包含两类项目：

```text
programs/
├─ AHU程序/
│  ├─ 三山经开区/flows_20251210190941.json
│  ├─ 云霞县华龙科技/flows_20251229175702.json
│  └─ 泰安宁阳中医院/flows_20260206160555.json
└─ 机房群控程序/
   ├─ 20230707_机房群控示例程序{2主机+3冷冻冷却水泵+2冷却塔].json
   └─ 风冷热泵标准控制程序[风冷涡旋]20240905.json
```

这些 JSON 的共同结构：

- 顶层是扁平数组。
- `tab` 表示页面。
- `subflow` 表示子流程模板。
- `type = subflow:<id>` 表示子流程实例。
- 普通节点通过 `z` 字段归属到页面或子流程。
- `wires` 在本项目 JSON 中按输入端口记录上游节点引用。

### `文档/`

存放项目资料和分析文档。

```text
文档/
├─ 1.命名规则v20191011B.XLSX
├─ AHU程序结构对比总结.md
└─ 机房群控程序JSON结构对比总结.md
```

重点：

- `1.命名规则v20191011B.XLSX` 是人工维护的命名规则源文件。
- 运行时不直接频繁读取 Excel，而是编译为 `data/naming_rules.sqlite` 和 `data/naming_rules.json`。
- 两份结构总结文档说明了 AHU、机房群控、风冷热泵程序的结构差异。

### `midea_flowchart/`

Python 后端工具包。

```text
midea_flowchart/
├─ __init__.py
├─ cli.py
├─ flow_parser.py
├─ xlsx_reader.py
├─ naming_rules.py
├─ acceptance.py
└─ diagram_builder.py
```

各文件职责：

- `cli.py`：命令行入口。
- `flow_parser.py`：解析 `programs` 下的 JSON，输出统一中间模型。
- `xlsx_reader.py`：用 Python 标准库读取 `.xlsx`，避免依赖 `openpyxl`。
- `naming_rules.py`：将命名规则 Excel 编译为 SQLite/JSON 索引，并提供点位匹配能力。
- `acceptance.py`：生成全量解析与命名验收报告。
- `diagram_builder.py`：将中间模型压缩为模块级抽象图数据。

### `configs/domain_profiles/`

领域抽象配置目录。

```text
configs/domain_profiles/
├─ ahu.json
├─ chiller_plant.json
└─ air_source_heat_pump.json
```

这些配置用于控制“节点如何归类为业务模块”。

- `ahu.json`：AHU 项目抽象规则。
- `chiller_plant.json`：传统机房群控项目抽象规则。
- `air_source_heat_pump.json`：风冷热泵标准程序抽象规则。

后续如果甲方认为模块粒度不合适，优先调整这里，而不是改前端硬编码。

### `data/`

命名规则运行时索引目录。

```text
data/
├─ naming_rules.sqlite
└─ naming_rules.json
```

由以下命令生成：

```powershell
conda activate midea
python -m midea_flowchart.cli accept --programs programs --excel "文档/1.命名规则v20191011B.XLSX" --data-dir data --out build
```

### `build/`

后端生成结果目录。

```text
build/
├─ acceptance_report.md
├─ acceptance_report.json
├─ diagram_report.md
├─ diagram_report.json
├─ intermediate/
└─ diagrams/
```

说明：

- `acceptance_report.*`：解析、命名索引、点位匹配、quote 回溯的验收报告。
- `diagram_report.*`：抽象图生成结果和模块压缩指标。
- `intermediate/*.json`：统一中间模型。
- `diagrams/*.json`：前端可消费的抽象图数据。

### `web/`

React + React Flow 前端原型。

```text
web/
├─ package.json
├─ package-lock.json
├─ vite.config.js
├─ index.html
├─ scripts/
│  └─ prepare-data.mjs
├─ public/
│  └─ data/
└─ src/
   ├─ main.jsx
   └─ styles.css
```

职责：

- `scripts/prepare-data.mjs`：将 `build/diagrams/*.json` 同步到 `web/public/data/`，并生成 `manifest.json`。
- `src/main.jsx`：React 应用主逻辑，包含项目列表、总览图、页面图、右侧详情面板。
- `src/styles.css`：前端样式，当前采用 Data-Dense + Drill-Down 工程工作台风格。

前端运行：

```powershell
cd web
npm install
npm run dev
```

默认地址：

```text
http://127.0.0.1:5173/
```

### `tests/`

验收测试目录。

```text
tests/
├─ __init__.py
└─ test_acceptance.py
```

测试覆盖：

- 5 个原始 JSON 是否能解析为统一模型。
- 命名规则 Excel 是否能编译为 SQLite/JSON 索引。
- 全量验收报告是否通过。
- 抽象图是否能为所有项目生成，并控制页面模块数量。

## 常用命令

### 生成命名索引、中间模型和验收报告

```powershell
conda activate midea
python -m midea_flowchart.cli accept --programs programs --excel "文档/1.命名规则v20191011B.XLSX" --data-dir data --out build
```

### 生成抽象图数据

```powershell
conda activate midea
python -m midea_flowchart.cli build-diagrams --programs programs --rules-json data/naming_rules.json --profiles configs/domain_profiles --out build/diagrams
```

### 运行后端测试

```powershell
conda activate midea
python -m unittest discover -v
```

### 运行前端

```powershell
cd web
npm install
npm run dev
```

### 前端生产构建

```powershell
cd web
npm run build
```

## 当前验收指标

最近一次已知结果：

- 命名规则：`633` 条规则，`3038` 条别名索引。
- 项目数量：`5`。
- 所有项目 `quote` 回溯率：`100%`。
- 抽象图验收：通过。
- 前端构建：通过。

抽象图示例：

- 三山 AHU：总览 `4` 个模块，`3` 条总览边；控制页从 `327` 个原始节点压缩为 `19` 个模块。
- 风冷热泵标准程序：总览 `9` 个模块，`24` 条总览边；最大页面模块数 `18`。

## 重要设计约定

1. **不要只围绕三山 AHU 做专用逻辑**  
   所有解析、命名、抽象逻辑都要能跑完整 `programs` 下 5 个项目。

2. **Excel 是源文件，不是运行时查询文件**  
   命名规则 Excel 应先编译到 `data/naming_rules.sqlite` 和 `data/naming_rules.json`。

3. **抽象逻辑优先写在领域配置里**  
   模块归类、点位前缀、页面角色优先放在 `configs/domain_profiles/*.json`。

4. **前端只展示抽象图数据**  
   前端不直接解析原始 `programs/*.json`，只读取 `web/public/data/manifest.json` 和图数据 JSON。

5. **所有模块必须可追溯**  
   抽象模块中保留 `source_node_ids`，用于回查原始 JSON 节点。

6. **AI 后续用于解释和摘要，不负责底层结构判定**  
   点位匹配、图结构、连线聚合应保持确定性。AI 更适合生成模块摘要、解释未匹配点位、建议抽象粒度。

## 新对话接手建议

如果在新的 Codex 对话中继续开发，建议按以下顺序恢复上下文：

1. 读本文件 `项目结构.md`。
2. 读 `README.md` 获取最新运行命令。
3. 看 `build/diagram_report.md` 了解当前抽象效果。
4. 如果要改抽象规则，优先看 `configs/domain_profiles/*.json` 和 `midea_flowchart/diagram_builder.py`。
5. 如果要改前端，优先看 `web/src/main.jsx` 和 `web/src/styles.css`。
6. 改完后运行：

```powershell
conda activate midea
python -m unittest discover -v
cd web
npm run build
```

## 下一步方向

建议下一步优先做：

1. 在抽象图数据中补充完整 `matched_points` 和 `unmatched_points`，前端右侧详情可完整展示未匹配点位。
2. 为模块增加 `summary` 字段，先规则生成，后续可接 AI 摘要。
3. 以三山 AHU 对照 `示例.png` 做一次抽象粒度评审。
4. 根据评审反馈调整领域配置。
5. 增加导出 PNG/SVG/PDF 能力。
