# 项目结构

本文档用于帮助 Codex 和维护者快速理解仓库结构、主要数据流和维护边界。只记录长期稳定的信息。

## 总体分层

项目目标是把美的暖通/楼宇控制平台导出的 JSON 程序转换为可读的工程流程图数据，并通过 Web 前端展示。

核心链路：

```text
programs/*.json / 上传 JSON
        |
        v
midea_flowchart.parser
        |
        v
midea_flowchart.naming
        |
        v
midea_flowchart.abstraction
        |
        v
web 前端 / output 导出 JSON
```

后端负责确定性解析、命名匹配和抽象图构建；前端负责交互展示，不承担业务规则推断。

## 根目录

- `midea_flowchart/`：后端 Python 包，包含解析、profile、命名、抽象图构建和本地 HTTP 服务。
- `web/`：无构建工具的前端页面，使用原生 ES module。
- `configs/domain_profiles/`：领域 profile 配置，用于识别项目类型、页签角色、点位分组和声明式抽象图规则。
- `data/`：命名规则结构化数据。
- `programs/`：示例程序 JSON，按项目类型和项目名称组织。
- `scripts/`：命令行辅助脚本。
- `tests/`：单元测试和回归测试。
- `文档/`：原始资料和结构分析文档。
- `记录/`：设计方案和开发记录。
- `output/`：导出的中间视图 JSON 样例。

## 后端模块边界

### `midea_flowchart.models`

定义内部中间模型：

- `ProjectModel`
- `TabModel`
- `SubflowModel`
- `NodeModel`
- `EdgeModel`
- `PointModel`
- `ModuleInstanceModel`
- `Diagnostic`

维护边界：

- 这里应保持为纯数据结构。
- 不放解析逻辑、业务分类逻辑或展示逻辑。

### `midea_flowchart.parser`

负责把原始 JSON 转成 `ProjectModel`。

主要职责：

- 读取 JSON 文件或文本。
- 收集页签、子流程、节点、原始连线和点位。
- 识别 profile。
- 给页签分配角色。
- 生成解析诊断信息。

维护边界：

- 只处理原始数据到通用中间模型的转换。
- 不构建总览图、模块详情图或前端展示结构。
- 只在用户输入和外部 JSON 边界做必要验证。

### `midea_flowchart.profiles`

负责加载和使用 `configs/domain_profiles/*.json`。

主要职责：

- 加载领域 profile。
- 根据文件路径和页签名称识别项目类型。
- 根据 profile 分配页签角色。
- 根据 profile 给点位分组。

维护边界：

- profile 是领域配置入口。
- 新项目类型优先扩展 profile，不优先复制 Python 规则。

### `midea_flowchart.naming`

负责命名规则匹配。

主要职责：

- 加载 `data/naming_rules.json`。
- 标准化点位名称。
- 匹配命名规则。
- 标记 `matched`、`unmatched`、`conflict`、`ignored` 等状态。

维护边界：

- 不处理流程图结构。
- 不做业务模块识别。

### `midea_flowchart.abstraction`

后端抽象视图入口。

主要职责：

- 对外提供 `build_project_view(project)`。
- 汇总项目摘要、总览图、页签详情、命名异常和原始图。

维护边界：

- 作为编排层保持薄。
- 具体构图逻辑放到 `overview_builder.py`、`detail_graph_builder.py`、`dx_matrix.py`、`serializers.py`。

### `midea_flowchart.overview_builder`

负责构建第一层系统总览图。

主要职责：

- 对页签排序。
- 构建总览节点。
- 构建 profile 业务边或通用跨页引用边。

维护边界：

- AHU 的角色顺序、角色元数据和总览业务边从 profile 读取。
- 未知 profile 保留通用降级逻辑。

### `midea_flowchart.detail_graph_builder`

负责构建第二层模块详情图。

主要职责：

- 按页签角色生成详情图。
- 聚合原始节点到业务模块。
- 生成业务主链路和原始支撑连线。
- 保留 `directSupportCount`、`reverseSupportCount`、`supportDirection` 语义字段。

维护边界：

- 模块元数据、模块顺序和 profile 业务边从 profile 读取。
- 点位和子流程的启发式分类仍在 Python 中实现。
- `generic_groups` 是未知项目和未知页签的必要降级，不应删除。

### `midea_flowchart.ahu_rules`

AHU 领域辅助规则。

主要职责：

- 提供 profile 声明式字段的读取和默认兜底。
- 保留 AHU 点位和子流程实例的启发式分类。
- 提供固定布局坐标。

维护边界：

- 声明式规则优先放到 `configs/domain_profiles/ahu.json`。
- 复杂启发式逻辑暂时保留在 Python，避免把 JSON profile 变成隐式规则引擎。

### `midea_flowchart.dx_matrix`

负责直膨机状态矩阵。

主要职责：

- 根据点位前缀和寄存器信息组织外机状态矩阵。

维护边界：

- 只处理直膨状态矩阵，不承担通用详情图构建。

### `midea_flowchart.serializers`

负责把内部模型序列化为前端/API 使用的字典结构。

主要职责：

- 原始图序列化。
- 点位、模块、诊断、命名异常序列化。
- 页签原始追踪 `rawTrace` 序列化。

维护边界：

- 不做业务判断。
- 不访问 DOM 或前端状态。

### `midea_flowchart.server`

本地 HTTP 服务。

主要职责：

- 提供静态前端文件。
- 提供 `/api/samples`。
- 提供 `/api/analyze` 的 GET 示例解析和 POST 上传解析。

维护边界：

- 保持轻量，不引入业务逻辑。
- 解析流程应继续调用 `parser -> naming -> abstraction`。

## 前端模块边界

前端位于 `web/`，使用浏览器原生 ES module，无构建步骤。

- `index.html`：页面结构和脚本入口。
- `styles.css`：视觉样式。
- `app.js`：入口、事件绑定和页面级编排。
- `api.js`：后端 API 调用。
- `state.js`：全局前端状态和查询辅助函数。
- `canvas.js`：SVG 总览图、详情图和原始图渲染。
- `drag.js`：SVG 节点拖拽。
- `details.js`：右侧详情面板渲染。
- `dom.js`：DOM 引用、SVG/HTML 工具和颜色常量。

维护边界：

- 前端不推断业务规则。
- 前端只消费后端返回的 project view。
- SVG 绘图、详情面板、拖拽、API 调用应保持分离。

## Profile 配置

领域 profile 位于 `configs/domain_profiles/`。

当前稳定字段：

- `name`：profile 名称。
- `family_keywords` / `file_keywords`：项目类型识别关键字。
- `tab_roles`：页签角色识别。
- `point_groups`：点位分组识别。
- `overview`：总览图声明式规则。
- `detail`：详情图声明式规则。

AHU profile 中的声明式规则包括：

- 总览角色顺序。
- 总览角色标签和类型。
- 总览业务边。
- 详情模块标签和类型。
- 详情模块顺序。
- 详情业务边。
- 详情说明文本。

## 视图数据约定

`build_project_view()` 返回前端使用的统一结构：

- `project`：项目摘要。
- `overview`：第一层总览图。
- `tabs`：页签详情和第二层模块详情图。
- `namingIssues`：命名异常清单。
- `rawGraph`：第三层原始节点图。

每个 tab 内稳定包含：

- `summary`
- `points`
- `inputPoints`
- `outputPoints`
- `moduleInstances`
- `detailGraph`
- `statusMatrix`
- `rawTrace`
- `rawNodes`

`rawTrace` 和 `supportEdges` 当前属于稳定输出结构，前端和测试都依赖它们，不应随意删除。

## 测试与脚本

- `tests/test_phase1_phase2.py`：解析、命名和基础样例测试。
- `tests/test_phase3_phase4.py`：总览图、交互数据、详情图和 profile 声明式规则测试。
- `scripts/validate_phase1_phase2.py`：样例解析验证脚本。
- `scripts/export_project_view.py`：导出中间视图 JSON。

常用验证命令：

```powershell
conda activate midea
python -m unittest discover -s tests
python -m compileall midea_flowchart
python -m json.tool configs/domain_profiles/ahu.json
```

## 维护原则

- 解析层通用，抽象层可配置，展示层可降级。
- 未知页签、未知模块、未知 profile 应可见，不应静默丢弃。
- 新领域优先新增或扩展 profile。
- 只有声明式规则适合迁入 profile；复杂启发式分类保留在 Python，直到有明确的规则引擎设计。
- 不要为了不可能发生的内部情况添加回退；只在用户输入、文件、HTTP 请求等系统边界验证。
